[project]
name = "faststreamtemplate"
version = "0.1.0"
description = "Monorepo: common library + user (customer) and store (inventory) services"
readme = "README.md"
requires-python = ">=3.13"
dependencies = []

[tool.uv.workspace]
members = ["common", "services/user_service", "services/store_service"]

[dependency-groups]
dev = []
